package org.metaabm.gen.test;

import org.metaabm.gen.test.share.AbstractGenerateBaseTest;

/**
 * ***Note*** These unit tests assume that they are launched as an Eclipse JUnit
 * test case (headless) that include the enclosing projects in the launched
 * workspace.
 * 
 * @author milesparker
 */
public class GenerateAscapeTest extends AbstractGenerateBaseTest {

    // NOTE: in order for these tests to run correctly currently it is
    // neccessary to run them w/on a pre-prepared
    // workspace with the appropriate projects already loaded.
    public void testGenerate() {
        // executeGeneration("org.eclipse.amp.amf.gen.ascape.test", AbstractGenerateBaseTest.BASE_ACT_MOVE_MODEL_NAMES,
        // AbstractGenerateBaseTest.BASE_ACT_MOVE_CLASS_NAMES);
        assertTrue(true);
    }

}
